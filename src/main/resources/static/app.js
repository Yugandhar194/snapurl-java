const yearEl = document.getElementById("currentYear"); if (yearEl) yearEl.textContent = new Date().getFullYear();
const API=(window.__HATCHABLE__&&window.__HATCHABLE__.api)||"/api";
const STORE_KEY="snapurl.links.v1";
const THEME_KEY="snapurl.theme";
const $=s=>document.querySelector(s);
function readSaved(){try{return JSON.parse(localStorage.getItem(STORE_KEY)||"[]")}catch{return[]}}
function writeSaved(x){localStorage.setItem(STORE_KEY,JSON.stringify(x.slice(0,50)))}
function formatDate(v){if(!v)return"never";return new Intl.DateTimeFormat(undefined,{dateStyle:"medium",timeStyle:"short"}).format(new Date(v))}
function escapeHtml(v){return String(v||"").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#039;")}
function shortUrl(c){return location.origin+"/s/"+encodeURIComponent(c)}
function showToast(t,type){const e=$("#toast");if(!e)return;type=type||(/choose|failed|invalid|error|enter|something went wrong|cannot|unable/i.test(t)?"error":/downloaded|created|success|compressed|converted|enabled|disabled|deleted|copied/i.test(t)?"success":"info");e.textContent=t;e.className="toast "+type+" show";clearTimeout(showToast.timer);showToast.timer=setTimeout(()=>e.classList.remove("show"),2600)}
async function copyText(t){try{await navigator.clipboard.writeText(t);showToast("Copied!")}catch{showToast("Copy failed")}}
function applyTheme(t){document.documentElement.dataset.theme=t;localStorage.setItem(THEME_KEY,t);$("#themeToggle").textContent=t==="dark"?"☀":"◐"}
function initTheme(){const s=localStorage.getItem(THEME_KEY);applyTheme(s||(window.matchMedia("(prefers-color-scheme: dark)").matches?"dark":"light"))}
async function api(path,options={}){const r=await fetch(API+path,Object.assign({headers:Object.assign({"Content-Type":"application/json"},options.headers||{})},options));const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||"Something went wrong.");return d}
function renderStats(items){$("#linkCount").textContent=items.length;$("#clickCount").textContent=items.reduce((s,i)=>s+Number(i.link?.clickCount||0),0)}
async function refreshLinks(){const saved=readSaved(),panel=$("#linkList"),empty=$("#emptyState");panel.innerHTML="";if(!saved.length){empty.style.display="block";renderStats([]);return}empty.style.display="none";const resolved=[];for(const item of saved){try{const d=await api("/manage",{method:"POST",body:JSON.stringify({code:item.code,token:item.token})});resolved.push({...item,link:d.link})}catch{resolved.push({...item,link:null})}}const valid=resolved.filter(i=>i.link);if(valid.length!==resolved.length)writeSaved(valid.map(i=>({code:i.code,token:i.token})));renderStats(valid);valid.forEach(item=>{const l=item.link,u=shortUrl(l.code),active=l.active&&(!l.expiresAt||new Date(l.expiresAt)>new Date()),row=document.createElement("div");row.className="link-row";row.innerHTML='<div class="link-main"><div class="short-link">'+escapeHtml(u)+'</div><div class="destination" title="'+escapeHtml(l.destinationUrl)+'">'+escapeHtml(l.title||l.destinationUrl)+'</div><div class="link-meta"><span class="meta">'+Number(l.clickCount||0)+' clicks</span><span class="meta">created '+escapeHtml(formatDate(l.createdAt))+'</span><span class="status '+(active?"active":"off")+'">'+(active?"ACTIVE":"INACTIVE")+'</span>'+(l.expiresAt?'<span class="meta">expires '+escapeHtml(formatDate(l.expiresAt))+"</span>":"")+'</div></div><div class="row-actions"><button class="small-btn copy-btn">Copy</button><button class="small-btn open-btn">Open</button></div><div class="row-actions"><button class="small-btn toggle-btn">'+(active?"Disable":"Enable")+'</button><button class="small-btn delete-btn">Delete</button></div>';panel.appendChild(row);row.querySelector(".copy-btn").onclick=()=>copyText(u);row.querySelector(".open-btn").onclick=()=>window.open(u,"_blank","noopener");row.querySelector(".toggle-btn").onclick=async()=>{try{await api("/toggle",{method:"POST",body:JSON.stringify({code:l.code,token:item.token,active:!active})});showToast(active?"Link disabled":"Link enabled");refreshLinks()}catch(e){showToast(e.message)}};row.querySelector(".delete-btn").onclick=async()=>{if(!confirm("Delete this short link? This cannot be undone."))return;try{await api("/delete",{method:"POST",body:JSON.stringify({code:l.code,token:item.token})});writeSaved(readSaved().filter(i=>i.code!==l.code));showToast("Link deleted");refreshLinks()}catch(e){showToast(e.message)}}})}
let latestResult=""; let latestOriginal=""; let latestForm={url:"",alias:"",expiry:""};$("#themeToggle").onclick=()=>applyTheme(document.documentElement.dataset.theme==="dark"?"light":"dark");$("#shortenForm").onsubmit=async e=>{e.preventDefault();const b=$("#submitBtn"),m=$("#message"),r=$("#result");m.textContent="";r.classList.remove("show");b.disabled=true;b.textContent="Creating…";try{const d=await api("/shorten",{method:"POST",body:JSON.stringify({url:$("#url").value,alias:$("#alias").value,expiresInDays:$("#expiry").value?Number($("#expiry").value):null})});latestResult=shortUrl(d.link.code);latestOriginal=d.link.destinationUrl||$("#url").value;latestForm={url:$("#url").value,alias:$("#alias").value,expiry:$("#expiry").value};$("#resultUrl").textContent=latestResult;$("#originalToggle").textContent="↩ Edit original URL";r.classList.add("show");writeSaved([{code:d.link.code,token:d.manageToken},...readSaved().filter(i=>i.code!==d.link.code)]);$("#shortenForm").reset();showToast("Short link created");refreshLinks()}catch(e){m.textContent=e.message}finally{b.disabled=false;b.textContent="Shorten URL →"}};$("#copyResult").onclick=()=>copyText(latestResult);$("#originalToggle").onclick=()=>{$("#url").value=latestForm.url||latestOriginal;$("#alias").value=latestForm.alias||"";$("#expiry").value=latestForm.expiry||"";$("#result").classList.remove("show");$("#message").textContent="";$(".url-card").scrollIntoView({behavior:"smooth",block:"center"});setTimeout(()=>$("#url").focus(),350);showToast("Original URL restored","info")};$("#openResult").onclick=()=>window.open(latestResult,"_blank","noopener");$("#shareResult").onclick=async()=>navigator.share?await navigator.share({title:"Short link",url:latestResult}):copyText(latestResult);// File tools
function downloadBlob(blob,name){const a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download=name;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}
function setToolOutput(id,msg){const e=$(id);e.innerHTML=msg}
function loadExternalScript(url,globalName){return new Promise((resolve,reject)=>{if(window[globalName])return resolve(window[globalName]);const s=document.createElement("script");s.src=url;s.onload=()=>window[globalName]?resolve(window[globalName]):reject(new Error(globalName+" failed to initialize"));s.onerror=()=>reject(new Error("Could not load "+globalName));document.head.appendChild(s)})}
async function ensureWordPdfLibraries(){const mammoth=window.mammoth||await loadExternalScript("https://unpkg.com/mammoth@1.12.3/mammoth.browser.min.js","mammoth");const jspdf=window.jspdf||await loadExternalScript("https://unpkg.com/jspdf@2.5.2/dist/jspdf.umd.min.js","jspdf");return{mammoth,jspdf}}
var sizeInput=$("#imageSize"); var sizeError=$("#imageSizeError"); function validateImageSize(){var n=Number(sizeInput.value);var ok=Number.isInteger(n)&&n>=10&&n<=1000;if(sizeError)sizeError.textContent=ok?"":"Target size must be a whole number between 10 and 1000 KB.";sizeInput.setCustomValidity(ok?"":"Enter 10–1000 KB.");return ok} sizeInput.addEventListener("input",validateImageSize); validateImageSize();
async function compressToTarget(blobFactory,targetBytes){let low=.05,high=.95,best=null;for(let i=0;i<9;i++){const quality=(low+high)/2;const blob=await blobFactory(quality);if(blob.size<=targetBytes){best=blob;low=quality}else high=quality}return best||await blobFactory(.05)}
$("#compressImage").onclick=async()=>{const f=$("#imageInput").files[0];if(!f)return showToast("Choose an image first");const out=$("#imageOutput");var targetKB=Number(sizeInput.value);if(!validateImageSize()){sizeInput.reportValidity();showToast("Enter 10–1000 KB");return}out.textContent="Compressing…";try{const im=new Image();im.src=URL.createObjectURL(f);await im.decode();const max=2400,scale=Math.min(1,max/Math.max(im.width,im.height));const c=document.createElement("canvas");c.width=Math.max(1,Math.round(im.width*scale));c.height=Math.max(1,Math.round(im.height*scale));c.getContext("2d").drawImage(im,0,0,c.width,c.height);const type=/png/i.test(f.type)?"image/png":"image/jpeg";const blob=await compressToTarget(q=>new Promise(resolve=>c.toBlob(resolve,type,q)),Number(sizeInput.value)*1024);downloadBlob(blob,(f.name.replace(/\.[^.]+$/,"")||"image")+"-compressed."+(type==="image/png"?"png":"jpg"));out.textContent="Compressed: "+Math.round(blob.size/1024).toLocaleString()+" KB";showToast("Image compressed!")}catch(e){out.textContent="Compression failed. Try another image.";showToast("Compression failed")}};
function convertImageFormat(file,targetType){return new Promise((resolve,reject)=>{const url=URL.createObjectURL(file),im=new Image();im.onload=()=>{const c=document.createElement("canvas");c.width=im.naturalWidth;c.height=im.naturalHeight;const ctx=c.getContext("2d");if(targetType==="image/jpeg"){ctx.fillStyle="#fff";ctx.fillRect(0,0,c.width,c.height)}ctx.drawImage(im,0,0);c.toBlob(b=>b?resolve(b):reject(new Error("Could not create image")),targetType,.95);URL.revokeObjectURL(url)};im.onerror=()=>{URL.revokeObjectURL(url);reject(new Error("Invalid image"))};im.src=url})}
async function runImageConversion(targetType){const f=$("#imageConvertInput").files[0];if(!f)return showToast("Choose a JPG or PNG first");if(targetType==="image/png"&&f.type!=="image/jpeg")return showToast("Choose a JPG file for JPG → PNG");if(targetType==="image/jpeg"&&f.type!=="image/png")return showToast("Choose a PNG file for PNG → JPG");const out=$("#imageConvertOutput");out.textContent="Converting…";try{const b=await convertImageFormat(f,targetType);downloadBlob(b,(f.name.replace(/\.[^.]+$/,"")||"image")+"."+(targetType==="image/png"?"png":"jpg"));out.textContent="Done — "+Math.round(b.size/1024).toLocaleString()+" KB downloaded.";showToast("Image converted!")}catch(e){out.textContent="Conversion failed: "+e.message;showToast("Image conversion failed")}}
$("#jpgToPng").onclick=()=>runImageConversion("image/png");$("#pngToJpg").onclick=()=>runImageConversion("image/jpeg");
async function imageToPdf(file,inputType,outId){if(!file)return showToast("Choose an image first");const out=$(outId);out.textContent="Converting…";try{const libs=await ensureWordPdfLibraries(),{jsPDF}=libs.jspdf,url=URL.createObjectURL(file),im=new Image();im.src=url;await im.decode();const orientation=im.naturalWidth>=im.naturalHeight?"landscape":"portrait",pdf=new jsPDF({unit:"mm",format:"a4",orientation}),pw=pdf.internal.pageSize.getWidth(),ph=pdf.internal.pageSize.getHeight(),m=10,s=Math.min((pw-2*m)/im.naturalWidth,(ph-2*m)/im.naturalHeight),w=im.naturalWidth*s,h=im.naturalHeight*s;pdf.addImage(im,inputType==="image/png"?"PNG":"JPEG",(pw-w)/2,(ph-h)/2,w,h);pdf.save(file.name.replace(/\.[^.]+$/,"")+".pdf");URL.revokeObjectURL(url);out.textContent="Done — PDF downloaded.";showToast("Image converted to PDF!")}catch(e){out.textContent="Conversion failed: "+e.message;showToast("Image → PDF failed")}}
$("#jpgToPdf").onclick=()=>imageToPdf($("#jpgPdfInput").files[0],"image/jpeg","#jpgPdfOutput");$("#pngToPdf").onclick=()=>imageToPdf($("#pngPdfInput").files[0],"image/png","#pngPdfOutput");
$("#wordToPdf").onclick=async()=>{const f=$("#wordToPdfInput").files[0];if(!f)return showToast("Choose a Word file first");const out=$("#wordPdfOutput");out.textContent="Converting…";try{const libs=await ensureWordPdfLibraries();const result=await libs.mammoth.extractRawText({arrayBuffer:await f.arrayBuffer()});const text=result.value||"";if(!text.trim())throw new Error("No readable text found in DOCX");const {jsPDF}=libs.jspdf;const pdf=new jsPDF({unit:"mm",format:"a4"});const margin=15,width=180,lineHeight=6;let y=18;for(const para of text.split(/\n+/)){const lines=pdf.splitTextToSize(para.trim(),width);if(!lines.length)continue;for(const line of lines){if(y>282){pdf.addPage();y=18}pdf.text(line,margin,y);y+=lineHeight}y+=2}pdf.save(f.name.replace(/\.docx$/i,"")+".pdf");out.textContent="Done — PDF downloaded.";showToast("Word converted!")}catch(e){console.error(e);out.textContent="Conversion failed: "+e.message;showToast("Word → PDF failed")}};
$("#pdfToWord").onclick=async()=>{const f=$("#pdfToWordInput").files[0];if(!f)return showToast("Choose a PDF first");const out=$("#pdfWordOutput");out.textContent="Extracting text…";try{if(!window.pdfjsLib||!window.docx)throw new Error("Conversion libraries did not load");window.pdfjsLib.GlobalWorkerOptions.workerSrc="https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.worker.min.js";const doc=await window.pdfjsLib.getDocument({data:await f.arrayBuffer()}).promise;const paras=[];for(let i=1;i<=doc.numPages;i++){const page=await doc.getPage(i),content=await page.getTextContent();let text=content.items.map(x=>x.str).join(" ").replace(/\s+/g," ").trim();if(text)paras.push(text)}if(!paras.length)throw new Error("No selectable text found. This PDF may be scanned/image-only.");const {Document,Packer,Paragraph}=window.docx;const d=new Document({sections:[{children:paras.map(t=>new Paragraph({text:t}))}]});const blob=await Packer.toBlob(d);downloadBlob(blob,f.name.replace(/\.pdf$/i,"")+".docx");out.textContent="Done — Word file downloaded.";showToast("PDF converted!")}catch(e){console.error(e);out.textContent="Conversion failed: "+e.message;showToast("PDF → Word failed")}};

document.querySelectorAll(".tool-card-3d").forEach(card=>{card.addEventListener("click",e=>{if(e.target.closest("button,input,select,a"))return;card.classList.remove("card-glow");void card.offsetWidth;card.classList.add("card-glow");const page=card.dataset.toolPage;if(page)setTimeout(()=>location.href=page,120)})});

document.addEventListener("DOMContentLoaded",()=>{initTheme();refreshLinks();});